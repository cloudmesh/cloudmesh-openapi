
openapi: 3.0.0
info:
  title: get_processor_name
  description: "The name of the processor"
  version: "1.0"
servers:
  - url: http://localhost:8080/cloudmesh
    description: "The name of the processor"
paths:
  /get_processor_name:
     get:
      summary: "The name of the processor"
      description: Optional extended description in CommonMark or HTML.
      operationId: cpu_basic_auth_enabled.get_processor_name
        
      responses:
        '200':
          description: "OK"
          content:
            text/plain:
              schema:
                type: string



components:
  securitySchemes:
    basicAuth: # <-- arbitrary name for the security scheme
      type: http
      scheme: basic
      x-basicInfoFunc: cpu_basic_auth_enabled.basic_auth

security:
  - basicAuth: [] # <-- use the same name here

