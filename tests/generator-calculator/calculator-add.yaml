
openapi: 3.0.0
info:
  title: add
  description: adding float and float.
  version: "1.0"
servers:
  - url: http://localhost:8080/cloudmesh
    description: adding float and float.
paths:
  /add:
     get:
      summary: adding float and float.
      description: Optional extended description in CommonMark or HTML.
      operationId: calculator.add
      parameters:
        - in: query
          name: x
          description: x value
          schema:
            type: number
        - in: query
          name: y
          description: y value
          schema:
            type: number
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: number

