openapi: 3.0.0
info:
  title: azure-ai-image-function
  description: No description provided
  version: "1.0"
servers:
  - url: http://localhost:8080/cloudmesh
    description: No description provided
paths:
  /azure-ai-image-function/get_image_category:
     get:
      summary: adding float and float.
      description: None (Optional extended description in CommonMark or HTML)
      operationId: azure-ai-image-function.get_image_category
      parameters:
        - in: query
          name: remote_image_url
          description: x value
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string

  /azure-ai-image-function/get_image_desc:
     get:
      summary: adding float and float.
      description: None (Optional extended description in CommonMark or HTML)
      operationId: azure-ai-image-function.get_image_desc
      parameters:
        - in: query
          name: remote_image_url
          description: x value
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string

  /azure-ai-image-function/get_image_tags:
     get:
      summary: adding float and float.
      description: None (Optional extended description in CommonMark or HTML)
      operationId: azure-ai-image-function.get_image_tags
      parameters:
        - in: query
          name: remote_image_url
          description: x value
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string