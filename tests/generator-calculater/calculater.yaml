
openapi: 3.0.0
info:
  title: calculater
  description: No description provided
  version: "1.0"
servers:
  - url: http://localhost/cloudmesh
    description: No description provided
paths:
  /calculater/add:
     get:
      summary: adding float and float.
      description: None (Optional extended description in CommonMark or HTML)
      operationId: calculater.add
      parameters:
        - in: query
          name: x
          description: x value
          schema:
            type: number
        - in: query
          name: y
          description: y value
          schema:
            type: number
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: number

  /calculater/division:
     get:
      summary: division float and float sample.
      description: None (Optional extended description in CommonMark or HTML)
      operationId: calculater.division
      parameters:
        - in: query
          name: x
          description: x value
          schema:
            type: number
        - in: query
          name: y
          description: y value
          schema:
            type: number
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: number

  /calculater/multiply:
     get:
      summary: Multiply float and float sample.
      description: None (Optional extended description in CommonMark or HTML)
      operationId: calculater.multiply
      parameters:
        - in: query
          name: x
          description: x value
          schema:
            type: number
        - in: query
          name: y
          description: y value
          schema:
            type: number
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: number

  /calculater/subtract:
     get:
      summary: subtract int and float sample.
      description: None (Optional extended description in CommonMark or HTML)
      operationId: calculater.subtract
      parameters:
        - in: query
          name: x
          description: x value
          schema:
            type: number
        - in: query
          name: y
          description: y value
          schema:
            type: number
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: number

